/**
 * Console UI kit — re-exported from the shared @orbisave/admin-ui package.
 *
 * The implementations moved to packages/admin-ui so Console and Manager render
 * from one source. This file keeps the '@/components/ui' import path every
 * Console page already uses. Console-only pieces (TrendAreaChart) stay as
 * sibling files in this directory.
 */
export * from '@orbisave/admin-ui'
